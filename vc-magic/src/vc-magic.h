/*
	file:
		vc-magic.h
	desc:
		This is the header file for vc-magic.cpp
*/
#include <stdio.h>
#include <windows.h>

#ifndef VCMAGIC_H
#define VCMAGIC_H
/*
void print_my1(char *format, ...) {
	char s[100];
	va_list argptr;
	va_start(argptr, format);
	vsprintf_s(s, format, argptr);
	va_end(argptr);
	MessageBox(0, s, s, 0);
}
*/
#endif